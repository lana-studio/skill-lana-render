---
name: Capability request
about: Something the skills cannot do because the MCP server has no tool for it
title: "[capability] "
labels: capability
---

Use this when the blocker is **on the server side** — something that would need a new or
extended `lana_*` tool. If a script is simply broken, file a skill bug instead.

## What you are trying to do

<!-- The editing goal, not the implementation. "Put a headline behind the person", not
     "expose a segmentation endpoint". -->

## What is missing today

<!-- Which tool you tried, and what it could not give you. If you got an error code, include it. -->

## Why it cannot be done on your own machine

<!-- This matters: the whole design is that heavy work runs on Lana so that any computer can
     produce a reel. A request that would require a GPU locally is the kind this project
     wants to move to the server — say so. -->

## How often you need it

<!-- Once, every video, occasionally? -->
