// DEVELOPMENT PLACEHOLDER — see plan.ts for why this ships as a `.ts`
// module instead of JSON. `make_pkg.py` emits `lana-pkg/src/ritmo.ts` with
// the same export signature.
import type { Ritmo } from "./types";
export default ({"titles":[],"punch":[],"bw":[],"glitch":[],"closingMs":0,"hookBanner":"","hookTag":"","hookEndMs":0}) as unknown as Ritmo;
